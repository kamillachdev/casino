#include <iostream>
#include <windows.h>
#include <conio.h>
#pragma once

using std::string;

class user
{
public:
	void showAccountBalance();
	void addBalance(double balance);
	void subtractBalance(double balance);
};

